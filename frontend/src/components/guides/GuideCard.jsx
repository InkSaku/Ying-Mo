import { Link } from 'react-router-dom'
const category = { skill_position: '技能点位', turret_position: '炮台点位', grenade_throw: '手雷投掷', detonator_throw: '雷管投掷', hold_angle: '架枪位置', defense_position: '防守站位', attack_route: '进攻路线', opening_tip: '开局技巧', energy_gain: '能量获取', team_composition: '阵容配合', map_mechanic: '地图机制', other: '其他技巧' }
function labelCategory(value) { return category[value] || value }
export default function GuideCard({ guide }) { return <article className="guide-card"><Link to={`/guide/${guide.id}`}><div className="guide-card__media">{guide.cover_image ? <img src={guide.cover_image} alt={`${guide.title} 封面步骤`} loading="lazy" /> : <span>映</span>}</div><div><p className="eyebrow">{labelCategory(guide.category)} · {guide.validity_status === 'valid' ? '当前有效' : guide.validity_status === 'invalid' ? '已失效' : '未验证'}</p><h3>{guide.title}</h3><p>{guide.game.name_zh}{guide.hero ? ` · ${guide.hero.name_zh}` : ''}{guide.map ? ` · ${guide.map.name_zh}` : ''}</p><p>{guide.excerpt}</p><small>{guide.author.nickname} · {guide.step_count} 步 · {guide.game_version || '版本未标注'}</small></div></Link></article> }
