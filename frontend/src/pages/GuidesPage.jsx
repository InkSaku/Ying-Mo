import { useEffect, useState } from 'react'
import { useSearchParams } from 'react-router-dom'
import { getGuides } from '../api/guides.js'
import GuideCard from '../components/guides/GuideCard.jsx'
import Pagination from '../components/life/Pagination.jsx'
export default function GuidesPage() { const [search, setSearch] = useSearchParams(); const query = search.get('query') || ''; const page = Math.max(1, Number(search.get('page')) || 1); return <GuideList key={query} query={query} page={page} search={search} setSearch={setSearch} /> }
function GuideList({ query, page, search, setSearch }) { const [input, setInput] = useState(query); const [state, setState] = useState({ key: null, data: [], pagination: null, error: null }); const [retry, setRetry] = useState(0); const filters = Object.fromEntries(['game_slug', 'hero_slug', 'map_slug', 'category', 'side', 'difficulty', 'validity_status', 'game_version', 'sort'].map((key) => [key, search.get(key) || ''])); const key = `${query}:${page}:${JSON.stringify(filters)}:${retry}`; useEffect(() => { const timer = setTimeout(() => { if (input !== query) { const next = new URLSearchParams(search); input.trim() ? next.set('query', input.trim()) : next.delete('query'); next.delete('page'); setSearch(next) } }, 350); return () => clearTimeout(timer) }, [input, query, search, setSearch]); useEffect(() => { let stop = false; getGuides({ query, page, page_size: 12, ...filters, sort: filters.sort || 'latest' }).then((result) => !stop && setState({ key, data: result.data, pagination: result.meta.pagination, error: null })).catch((error) => !stop && setState({ key, data: [], pagination: null, error })); return () => { stop = true } }, [query, page, filters, key]); const loading = state.key !== key; function update(values) { const next = new URLSearchParams(search); Object.entries(values).forEach(([name, value]) => value ? next.set(name, value) : next.delete(name)); if (!Object.hasOwn(values, 'page')) next.delete('page'); setSearch(next) } const error = loading ? null : state.error; return <section className="guides-page page-container"><p className="eyebrow">游戏教材</p><h1>把实战经验讲清楚</h1><div className="guide-filters"><input value={input} placeholder="搜索教材" onChange={(event) => setInput(event.target.value)} /><select value={filters.category} onChange={(event) => update({ category: event.target.value })}><option value="">全部分类</option><option value="skill_position">技能点位</option><option value="grenade_throw">手雷投掷</option><option value="other">其他技巧</option></select><select value={filters.difficulty} onChange={(event) => update({ difficulty: event.target.value })}><option value="">全部难度</option><option value="beginner">入门</option><option value="intermediate">中等</option><option value="advanced">进阶</option></select><select value={filters.sort || 'latest'} onChange={(event) => update({ sort: event.target.value === 'updated' ? 'updated' : '' })}><option value="latest">最新发布</option><option value="updated">最近更新</option></select></div>{loading && <p className="state-message">正在加载教材…</p>}{error && <div className="state-message state-message--error"><p>{error.message}</p><button onClick={() => setRetry((item) => item + 1)}>重新加载</button></div>}{!loading && !error && !state.data.length && <p className="life-empty">还没有符合条件的教材。</p>}{!loading && !error && <div className="guide-grid">{state.data.map((guide) => <GuideCard key={guide.id} guide={guide} />)}</div>}<Pagination pagination={loading ? null : state.pagination} onPageChange={(next) => update({ page: String(next) })} /></section> }
