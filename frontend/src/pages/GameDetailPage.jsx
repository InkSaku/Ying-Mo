import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { getGame } from '../api/games.js'
import { HeroCard, MapCard } from '../components/games/CatalogCards.jsx'

export default function GameDetailPage() { const { gameSlug } = useParams(); const [state, setState] = useState({ loading: true, data: null, error: null }); const [retry, setRetry] = useState(0); useEffect(() => { let stop = false; getGame(gameSlug).then((data) => !stop && setState({ loading: false, data, error: null })).catch((error) => !stop && setState({ loading: false, data: null, error })); return () => { stop = true } }, [gameSlug, retry]); if (state.loading) return <section className="games-page page-container"><p className="state-message">正在打开游戏…</p></section>; if (state.error) return <section className="games-page page-container"><div className="state-message state-message--error"><p>{state.error.status === 404 ? '没有找到这款游戏。' : state.error.message}</p><button onClick={() => setRetry((item) => item + 1)}>重新加载</button></div></section>; const game = state.data; return <section className="games-page page-container"><Link className="text-link" to="/games">返回游戏目录</Link><header className="game-hero">{game.cover_url ? <img src={game.cover_url} alt={`${game.name_zh} 封面`} /> : <div className="game-hero__placeholder">映</div>}<div>{game.icon_thumbnail_url && <img className="game-hero__icon" src={game.icon_thumbnail_url} alt={`${game.name_zh} 图标`} />}<p className="eyebrow">{game.current_version || '当前版本未标注'}</p><h1>{game.name_zh}</h1>{game.name_en && <p className="game-card__english">{game.name_en}</p>}<p>{game.description || '这款游戏还没有简介。'}</p>{game.aliases.length > 0 && <div className="tag-row">{game.aliases.map((alias) => <span key={alias}>{alias}</span>)}</div>}<p>{game.hero_count} 位英雄 · {game.map_count} 张地图</p><div className="life-toolbar"><Link className="button" to={`/guides?game_slug=${game.slug}`}>查看教材</Link><Link className="button button--primary" to={`/guide/create?game=${game.slug}`}>发布该游戏教材</Link></div></div></header><section className="catalog-section"><div className="catalog-section__heading"><h2>英雄预览</h2><Link className="text-link" to={`/game/${game.slug}/heroes`}>查看全部英雄</Link></div>{game.featured_heroes.length ? <div className="catalog-grid">{game.featured_heroes.map((hero) => <HeroCard key={hero.id} hero={hero} />)}</div> : <p className="life-empty">暂时还没有公开英雄。</p>}</section><section className="catalog-section"><div className="catalog-section__heading"><h2>地图预览</h2><Link className="text-link" to={`/game/${game.slug}/maps`}>查看全部地图</Link></div>{game.featured_maps.length ? <div className="catalog-grid">{game.featured_maps.map((map) => <MapCard key={map.id} map={map} />)}</div> : <p className="life-empty">暂时还没有公开地图。</p>}</section></section> }
